# TestGuide 與現有實作差異分析

## 主要差異對照表

| 項目 | TestGuide.txt | 現有實作 | 狀態 |
|------|--------------|----------|------|
| **函數名稱** |
| 購買 NFT | `deposit(amount)` | `buyNFT(amount)` | ⚠️ 名稱不同 |
| 年度結算 | `triggerYearlyUpdate()` | `SafeHarvestCalculator()` | ⚠️ 名稱不同 |
| 投資人查詢 | `getInvestorData(address)` | ❌ 不存在 | 🚫 缺失 |
| 專案狀態 | `getProjectStatus()` | ❌ 不存在 | 🚫 缺失 |
| **測試流程** |
| 初始資金 | deployer 分配 1000 TWDT 給投資人 | deployer 自己購買全部 NFT | ⚠️ 流程不同 |
| NFT 轉移 | 投資人直接購買 | 銀行購買後轉移 | ⚠️ 流程不同 |
| **測試場景** |
| Scenario 1 | 單一投資人 | ✅ 已覆蓋 |
| Scenario 2 | 多投資人分紅 | ❌ 未測試 | 🚫 缺失 |

## 建議方案

### 方案 1：新增 TestGuide 測試（推薦）
**優點**：
- 不影響現有測試
- 可以測試更多場景
- 符合 TestGuide 設計理念

**需要做的**：
1. 新增 `getInvestorData()` 和 `getProjectStatus()` 查詢函數
2. 新增測試檔案按 TestGuide 測試
3. 測試多投資人場景

### 方案 2：改名以符合 TestGuide
**優點**：
- 完全符合 TestGuide
- 統一名稱

**缺點**：
- 需要修改合約（可能影響現有測試）
- 需要更新所有引用

### 方案 3：混合方案
**優點**：
- 保留現有設計
- 補充缺失功能

**需要做的**：
1. 新增查詢函數
2. 新增多投資人測試
3. 註解說明函數對應關係

## 我的建議

**選擇方案 1**，因為：
1. 現有測試已經通過，不要破壞
2. TestGuide 的測試場景很有價值
3. 可以讓測試覆蓋更全面

**執行步驟**：
1. 先新增查詢函數到 SafeHarvestNFT.sol
2. 根據 TestGuide 建立新測試檔案
3. 測試多投資人場景
4. 驗證所有測試通過

## 需要討論的問題

1. **是否新增 `getInvestorData()` 和 `getProjectStatus()`？**
   - 這些函數對前端很有用
   - 可以方便查詢數據

2. **是否保留現有測試流程？**
   - 現有測試使用「銀行預付」模式
   - TestGuide 使用「投資人直接購買」模式
   - 建議兩者都保留

3. **是否實作 TestGuide 的多投資人測試？**
   - 這是 TestGuide 的核心測試場景
   - 建議實作

## 待您決定

請告訴我：
1. 想用哪個方案？
2. 是否同意新增查詢函數？
3. 是否保留現有測試？

我會根據您的決定繼續執行。

